{
  "name": "@aurelia/electron",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "main": "dist-electron/main.cjs",
  "scripts": {
    "build": "bun run scripts/build.ts",
    "dev": "bun run scripts/dev.ts",
    "typecheck": "vue-tsc --build tsconfig.typecheck.json --pretty false && tsc -p tsconfig.node.json --noEmit --pretty false"
  },
  "dependencies": {
    "@aurelia/shared": "file:../../shared"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "electron": "^37.2.0",
    "esbuild": "^0.25.0"
  },
  "trustedDependencies": [
    "electron",
    "esbuild"
  ]
}
